# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# YOLO12s-Cascade-P2 (4-Scale Optimized)
# 优化策略: 引入 P2 层 (高分辨率)，构建 P2-P5 四尺度检测架构，极大增强微小目标检测能力

# Parameters
nc: 80 # number of classes
scales: # model compound scaling constants
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] 
  s: [0.50, 0.50, 1024] 
  m: [0.50, 1.00, 512] 
  l: [1.00, 1.00, 512] 
  x: [1.00, 1.50, 512] 

# YOLO12 Backbone (保持不变)
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4 (Backbone P2)
  - [-1, 2, C3k2, [256, False, 0.25]] # 2 (Backbone P2 Refined - 将被用于融合)
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]] # 4 (Backbone P3 Refined)
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 4, A2C2f, [512, True, 4]] # 6 (Backbone P4 Refined)
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 4, A2C2f, [1024, True, 1]] # 8 (Backbone P5 Refined)

# YOLO12 Head (扩展为支持 P2)
head:
  # === FPN Top-down flow (P5 -> P4 -> P3) ===
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, A2C2f, [512, False, -1]] # 11 (Neck P4)

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, A2C2f, [256, False, -1]] # 14 (Neck P3)

  # === 新增分支: 继续下探到 P2 (P3 -> P2) ===
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 15 Upsample P3
  - [[-1, 2], 1, Concat, [1]] # 16 cat backbone P2 (Layer 2)
  - [-1, 2, C3k2, [128, False, 0.25]] # 17 (Neck P2 - 极高分辨率特征)

  # === PANet Bottom-up flow (P2 -> P3 -> P4 -> P5) ===
  - [-1, 1, Conv, [128, 3, 2]] # 18 Downsample Neck P2
  - [[-1, 14], 1, Concat, [1]] # 19 cat Neck P3
  - [-1, 2, A2C2f, [256, False, -1]] # 20 (Final P3)

  - [-1, 1, Conv, [256, 3, 2]] # 21 Downsample P3
  - [[-1, 11], 1, Concat, [1]] # 22 cat Neck P4
  - [-1, 2, A2C2f, [512, False, -1]] # 23 (Final P4)

  - [-1, 1, Conv, [512, 3, 2]] # 24 Downsample P4
  - [[-1, 8], 1, Concat, [1]] # 25 cat Backbone P5
  - [-1, 2, C3k2, [1024, True]] # 26 (Final P5)

  # === 核心修改部分：级联 + 4尺度融合优化 ===

  # [Stage 1: Coarse Detect]
  # 作用：生成注意力参数 (Gaze Params)
  # 输入：Neck 的四层特征 [P2, P3, P4, P5] -> [17, 20, 23, 26]
  # 注意：代码会自动取列表第一个元素(P2)生成热力图，精度将大幅提升
  - [[17, 20, 23, 26], 1, CoarseDetect, [nc]] # 27

  # [Innovation 1: STN (DifferentiableGazeShift)]
  # 作用：基于 Stage 1 的参数对特征进行变换
  # 输入：[P2, P3, P4, P5] -> 输出 [P2_stn, P3_stn, P4_stn, P5_stn]
  - [[17, 20, 23, 26], 1, DifferentiableGazeShift, []] # 28

  # [Innovation 2: Context Fusion (GL_ContextBlock)]
  # 作用：融合 STN 的局部特征(这里取分辨率最高的 P2_stn) 和 全局上下文特征(P5)
  # 输入：[28, 26] (Local, Global)。根据代码逻辑，这会输出增强后的 P2 尺度特征
  - [[28, 26], 1, GL_ContextBlock, []] # 29 (Enhanced P2)

  # [Stage 2: Fine Detect (Optimized)]
  # 优化点：构建新的四尺度输入，利用增强后的 P2 检测微小目标
  # 输入 1: Layer 29 (增强版 P2，带有 STN 聚焦和全局上下文) -> 极小目标核心层
  # 输入 2: Layer 20 (原始 P3) -> 小目标
  # 输入 3: Layer 23 (原始 P4) -> 中目标
  # 输入 4: Layer 26 (原始 P5) -> 大目标
  - [[29, 20, 23, 26], 1, Detect, [nc]] # 30